import { RoleVisibleDirective } from './role-visible.directive';

describe('RoleVisibleDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleVisibleDirective();
    expect(directive).toBeTruthy();
  });
});
